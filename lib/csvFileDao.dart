import 'package:flutter/material.dart';
import 'package:csv/csv.dart';

class FileWriter extends StatelessWidget {
  const FileWriter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}